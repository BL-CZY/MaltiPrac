package com.blczy.maltiprac.navigation

enum class Route(val route: String) {
    HOME("home"),
    LISTENING("listening"),
    LISTENING_CATEGORIES("listening/categories"),
    LISTENING_PSM("listening/psm"),
}